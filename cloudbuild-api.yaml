steps:
  # Build the API Docker image
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-t"
      - "gcr.io/$PROJECT_ID/puda-api:$COMMIT_SHA"
      - "-t"
      - "gcr.io/$PROJECT_ID/puda-api:latest"
      - "-f"
      - "Dockerfile.api"
      - "."

  # Deploy to Cloud Run
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: gcloud
    args:
      - "run"
      - "deploy"
      - "puda-api"
      - "--image"
      - "gcr.io/$PROJECT_ID/puda-api:$COMMIT_SHA"
      - "--region"
      - "europe-west1"
      - "--platform"
      - "managed"

images:
  - "gcr.io/$PROJECT_ID/puda-api:$COMMIT_SHA"
  - "gcr.io/$PROJECT_ID/puda-api:latest"

options:
  logging: CLOUD_LOGGING_ONLY
